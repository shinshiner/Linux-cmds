### View the disk
* 查看磁盘剩余空间 `df -hl`
* 查看某目录大小 `sudo du -sh [target path]`

### View the CPU state
```
sudo apt-get install htop
htop
```

### View the GPU state

1) install the nvidia driver
2) run `nvidia-smi`
